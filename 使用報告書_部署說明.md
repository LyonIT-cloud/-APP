# IT 運維與監控系統 - 部署與使用報告書

本系統為企業內網主機監測與遠端控制中心，採用「Web 中控端」與「被控端 Agent」之雙向互動設計。

---

## 📂 系統結構說明

主要資料夾已進行精簡，各元件功能如下：

1. **`設備安全回報器.exe`** (位於用戶端目錄)
   * **作用**：安裝在員工電腦（被控端）的唯一程式。
   * **部署**：將此檔案複製到同仁電腦執行，勾選「開機自動啟動」並縮小化即可。
2. **`backend/`** (後端伺服器)
   * **作用**：中控監測的核心 API 與網頁託管服務。
3. **`index.html`, `app.js`, `style.css`, `assets/`**
   * **作用**：中控網頁控制台 (Web Dashboard) 的前端視覺介面與互動邏輯。
4. **`devices/`**
   * **作用**：儲存各端點心跳 JSON 檔案的資料夾。

---

## 🚀 部署與啟動步驟

### 第一步：中控伺服器啟動 (已在背景為您啟動)
如果您需要手動重啟或在其他主機部署中控端：
1. 進入 `backend` 目錄執行：
   ```bash
   node server.js
   ```
2. 在任意瀏覽器中輸入網址進入控制台：
   👉 **`http://localhost:3000`**

### 第二步：同仁電腦安裝（被控端）與防毒白名單排除
由於被控端 Agent（`設備安全回報器.exe`）具備【遠端背景指令執行、靜默螢幕畫面回傳、主動網路監聽】等系統控制能力，極易被防毒軟體（例如卡巴斯基 Kaspersky、Windows Defender 等）的啟發式掃描演算法誤判為風險程式，導致檔案被自動隔離、刪除或無法啟動。

為解決此問題並順利安裝，我們已附上自動排除白名單部署腳本 **`安裝與白名單設定腳本.ps1`**。

#### 💻 自動部署安裝 (推薦)：
1. 將 **`設備安全回報器.exe`** 與 **`安裝與白名單設定腳本.ps1`** 放置在同一個資料夾。
2. 在 **`安裝與白名單設定腳本.ps1`** 上按右鍵，選擇 **「以 PowerShell 執行」**（或開啟系統管理員 PowerShell 視窗切換至該目錄並執行此腳本）。
3. 該腳本將自動：
   * 在系統安全目錄 `C:\Program Files\TopdgiAgent` 建立專屬安裝目錄。
   * 將 `設備安全回報器.exe` 複製進去。
   * 自動向 Windows Defender 宣告排除此路徑，免除其掃描查殺。
   * 寫入開機啟動 Registry。
   * 自動在同仁桌面建立執行捷徑（雙擊即可開啟）。

#### 🛡️ 卡巴斯基 (Kaspersky) 企業端防毒排除指引：
若是公司使用 Kaspersky 企業防護系統，請由 IT 管理端（Kaspersky Security Center, KSC）或單機卡巴斯基設定：
1. 開啟卡巴斯基設定面板 -> **「安全」** -> **「排除項目與防護威脅」**。
2. 點選 **「管理排除項目」**，新增以下規則：
   * **路徑**：`C:\Program Files\TopdgiAgent\` (排除整個資料夾)
   * **檔案**：`C:\Program Files\TopdgiAgent\設備安全回報器.exe`
3. 點選 **「信任應用程式」**，新增 `設備安全回報器.exe`，並勾選：
   * `不要掃描開啟的檔案`
   * `不要監視應用程式活動`
   * `不要檢查網路流量`
4. 儲存套用後，再將 EXE 置入此目錄即可穩定永久常駐，防毒便不會再對其進行攔截或刪除。

---

## 🛠️ 遠端功能操作指引

開啟中控網址 `http://localhost:3000`，點擊地圖上的線上主機，再點擊 **「🛠️ 遠端管理與控制面板」**：

1. **🔍 系統資產盤點**：即時查看被控端的 CPU、RAM、硬碟健康度及 Windows 登錄檔中所有安裝的軟體清單。
2. **👁️ 螢幕畫面監看 (觀察者)**：點擊「開始監看」即可靜默取得被控端螢幕（每 1.2 秒更新，無感且不干擾同仁作業）。
3. **🐚 遠端命令列**：選擇 CMD 或 PowerShell 後，可直接輸入指令（如 `ipconfig`）並接收回傳值。
4. **📁 檔案派送與收集**：
   * **派送**：選擇本地檔案並設定目的路徑（如 `C:\Windows\Temp\installer.exe`），可勾選背景靜默安裝。
   * **收集**：輸入目的端檔案路徑（如 `.log` 檔），一鍵下載至您的瀏覽器。
5. **📣 公告訊息推播**：輸入多行公告文字，被控端電腦會彈出置頂強制警告卡片。
6. **🔌 電源控制**：遠端強制重新開機、關機，或發送網路喚醒 (WOL) 封包。
